interface:
  display_name: "足球比分竞猜"
  short_description: "联网检索并严格计算足球比分预测静态 HTML 报告"
  default_prompt: "Use $football-score-predictor to research this match, run strict numeric calculation, and generate an HTML score prediction report."
